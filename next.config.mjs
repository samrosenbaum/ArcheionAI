/** @type {import('next').NextConfig} */
const nextConfig = {
  // Temporarily disabled security headers to fix connection issue
  // We'll add them back more carefully after the app is working
}

export default nextConfig
